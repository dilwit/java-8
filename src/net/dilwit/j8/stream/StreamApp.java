package net.dilwit.j8.stream;

public class StreamApp {

    public static void main(String[] args) {

        // Stream vs Collection
        // Collection is pretty much in memory data structure
        // Stream is pipeline where elements are computed on demand. (not a data structure, does not support indexed access, support for lambdas)

        Streammm.createNewStream();

    }
}
